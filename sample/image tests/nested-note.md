# Nested note in a folder with a space

This file lives in `image tests/` — a directory whose name contains a space,
which exercises base-path escaping (`VaultDirBase`). Both images should render.

## Image in this same (spaced) folder

![SVG in the spaced folder](./nested.svg)

## Image one level up (parent folder)

![PNG from the parent folder](../swatch.png)
